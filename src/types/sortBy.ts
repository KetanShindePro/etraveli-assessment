export enum SortByOptions {
  TITLE = "title",
  RELEASE_DATE = "release_date",
  RATING = "rating",
  EPISODE = "episode_id",
}

export enum SortOrder {
  ASCENDING = "ascending",
  DESCENDING = "descending",
}
